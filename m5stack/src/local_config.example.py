# ローカル設定テンプレート
# このファイルをコピーして local_config.py を作成し、実際の値を入力してください。
# local_config.py は .gitignore 済みのため GitHub には公開されません。

# 観測スポット (km) — 吉原側が 0.0
TARGET_KM = 2.5

# WiFi 接続先 (複数登録可、上から順に試みる)
# 不要なら空リストのままでも動作します (WiFi同期がスキップされます)
WIFI_NETWORKS = [
    # ("自宅のSSID", "自宅のパスワード"),
    # ("スマホテザリングのSSID", "テザリングのパスワード"),
]
